---
import type { ResearchData } from '../../lib/data';

type Props = {
  research: ResearchData;
};

const { research } = Astro.props;
const updated = new Intl.DateTimeFormat('en', {
  timeZone: 'Asia/Tokyo',
  year: 'numeric',
  month: '2-digit',
}).format(new Date(research.updated));
const classes = research.tags.slice(0, 3).join(' · ');
---

<article class="thesis-card">
  <div class="thesis-card__wash" aria-hidden="true"></div>
  <span class="thesis-card__corner thesis-card__corner--tl" aria-hidden="true"></span>
  <span class="thesis-card__corner thesis-card__corner--br" aria-hidden="true"></span>

  <div class="thesis-card__stamp">
    <span>※ The Thesis</span>
    <i></i>
    <small>{updated}</small>
  </div>

  <div>
    <h2>
      Stress detection from <em>multi-modal</em> physiological signals.
    </h2>
    <p>What I'm building, on the bench and on paper.</p>
  </div>

  <div class="thesis-card__class">
    <span>cl.</span>
    <i></i>
    <small>{classes}</small>
  </div>
</article>

<style>
  .thesis-card {
    position: relative;
    display: flex;
    min-height: 0;
    overflow: hidden;
    flex-direction: column;
    justify-content: space-between;
    gap: var(--s-5);
    padding: 26px 30px;
    border: 1px solid rgba(52, 211, 153, 0.2);
    border-radius: var(--r-lg);
    background: var(--surface);
    box-shadow:
      0 0 28px rgba(52, 211, 153, 0.04),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
  }

  .thesis-card__wash {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(420px 220px at 100% 100%, rgba(52, 211, 153, 0.08), transparent 60%),
      linear-gradient(180deg, rgba(255, 255, 255, 0.005), transparent);
    pointer-events: none;
  }

  .thesis-card__corner {
    position: absolute;
    width: 16px;
    height: 16px;
    opacity: 0.9;
  }

  .thesis-card__corner--tl {
    top: 14px;
    left: 14px;
    border-top: 1px solid rgba(52, 211, 153, 0.45);
    border-left: 1px solid rgba(52, 211, 153, 0.45);
  }

  .thesis-card__corner--br {
    right: 14px;
    bottom: 14px;
    border-right: 1px solid rgba(52, 211, 153, 0.45);
    border-bottom: 1px solid rgba(52, 211, 153, 0.45);
  }

  .thesis-card > *:not(.thesis-card__wash, .thesis-card__corner) {
    position: relative;
  }

  .thesis-card__stamp,
  .thesis-card__class {
    display: flex;
    align-items: center;
    gap: var(--s-3);
  }

  .thesis-card__stamp > span {
    padding: 3px 10px;
    border: 1px solid rgba(52, 211, 153, 0.25);
    border-radius: 3px;
    background: rgba(52, 211, 153, 0.04);
    color: var(--accent-green);
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .thesis-card__stamp i,
  .thesis-card__class i {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, rgba(52, 211, 153, 0.2), transparent);
  }

  .thesis-card__stamp small,
  .thesis-card__class small {
    color: var(--text-dim);
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.1em;
  }

  .thesis-card h2 {
    margin: 0;
    color: #fff;
    font-family: var(--font-serif);
    font-size: clamp(24px, 2.3vw, 30px);
    font-weight: 400;
    letter-spacing: -0.005em;
    line-height: 1.18;
    text-wrap: pretty;
  }

  .thesis-card h2 em {
    color: var(--accent-green);
    font-style: italic;
  }

  .thesis-card p {
    max-width: 360px;
    margin: var(--s-3) 0 0;
    color: var(--text-muted);
    font-family: var(--font-serif);
    font-size: 13px;
    font-style: italic;
    line-height: 1.55;
  }

  .thesis-card__class > span {
    flex-shrink: 0;
    color: var(--accent-green);
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .thesis-card__class small {
    overflow-wrap: anywhere;
  }
</style>
